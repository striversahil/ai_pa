{
  "id": "crm",
  "name": "CRM — Active Sales Orders",
  "description": "Pipeline of open Zoho Books sales orders grouped by the next pending process step (Confirm → Invoice → Ship → Payment). Fetched by the GH runner, served from KV.",
  "type": "handler",
  "trigger": { "type": "schedule", "cron": "*/15 * * * *" },
  "scope": "zoho",
  "enabled": true
}
